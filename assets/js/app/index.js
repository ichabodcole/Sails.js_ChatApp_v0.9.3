require(['views/LoginView', 'views/SignupView'], function(LoginView, SignupView) {

  // instantiate the messages collection.
  var loginView  = new LoginView();
  var signupView = new SignupView();
});